# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TextXib' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for TextXib
  
  pod "Alamofire"
  pod 'Kingfisher', '~> 7.0'
  target 'TextXibTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'TextXibUITests' do
    # Pods for testing
  end
  
end
